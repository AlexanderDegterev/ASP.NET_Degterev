@model IEnumerable<StatisticsOfSales.Domain.Entities.Sale>

@{
    ViewBag.Title = "List";
}

@foreach (var p in Model) 
{ 
  <div class="item"> 
    <h3>@p.Name</h3> 
      <h4>@p.Product</h4>
      <h4>@p.Price.ToString("c")</h4>
  </div> 
} 
